spring:
  application:
    name:customer_registration

  datasource:
    url: jdbc:mysql://localhost:3306/customerdb
    username: root
    password: Basava@2025
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8087
